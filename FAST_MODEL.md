# Q2 轻量测点选择模型

## 1. 模型目标

已知第一次检测点 $A$、示向度 $\hat\theta_1$ 和测向误差上限 $\varepsilon$，选择第二检测点 $B$，要求：

1. 在最小有效接收半径下仍能收到信号；
2. 两次示向交会后的最坏定位区域尽量小；
3. 计算足够快，可以被 Q3 动态决策循环重复调用。

## 2. 第一次观测后的源位置区域

设目标区域为 $\mathcal T$，接收半径满足

$$
R\in[R_{\min},R_{\max}].
$$

第一次返回 direction 后，干扰源位置集合为

$$
\mathcal P_1
=
\mathcal T
\cap
\mathcal W(A,\hat\theta_1,\varepsilon)
\cap
\overline D(A,R_{\max})
\setminus
D(A,r_{\mathrm{near}}),
$$

其中 $\mathcal W$ 是示向角扇形，$\overline D$ 是闭圆盘。

快速算法使用凸多边形外包络

$$
\mathcal P_1\subseteq\mathcal P_1^+.
$$

代码用测向半平面以及目标圆、最大接收圆的外切多边形构造 $\mathcal P_1^+$。近场圆不从外包络中扣除，因此安全判断更保守。

## 3. 第二检测点的保守安全域

定义候选点 $B$ 的接收安全余量

$$
m(B)
=
R_{\min}
-
\max_{s\in\mathcal P_1^+}\|B-s\|.
$$

保守安全候选域为

$$
\mathcal U
=
\{B:m(B)\ge 0\}.
$$

因为 $\mathcal P_1^+$ 是凸多边形，最大距离可在顶点集合上计算：

$$
m(B)
=
R_{\min}
-
\max_{v\in V(\mathcal P_1^+)}\|B-v\|.
$$

所以 $B\in\mathcal U$ 能保证对真实集合 $\mathcal P_1$ 中所有源位置都满足最小接收距离要求。

若考虑导航和通信误差，可加入物理缓冲 $b>0$：

$$
m(B)\ge b.
$$

当前快速代码取 $b=0$，后续 Q3 应根据机器狗定位误差设置缓冲。

## 4. 最坏定位直径目标

对可能源位置 $s$ 和第二次测向误差 $e\in[-\varepsilon,\varepsilon]$，第二次示向为

$$
\hat\theta_2(B,s,e)
=
\arg(s-B)+e.
$$

两次观测后的后验位置区域为

$$
\mathcal P_2(B;s,e)
=
\mathcal P_1^+
\cap
\mathcal W(B,\hat\theta_2(B,s,e),\varepsilon).
$$

以区域直径衡量剩余不确定性：

$$
D(B;s,e)
=
\operatorname{diam}\bigl(\mathcal P_2(B;s,e)\bigr).
$$

若 $\|B-s\|\le r_{\mathrm{near}}$，则已经进入近场清除范围，规定 $D(B;s,e)=0$。

主优化模型为

$$
B^*
\in
\arg\min_{B\in\mathcal U}J(B),
\qquad
J(B)
=
\sup_{s\in\mathcal P_1,\ |e|\le\varepsilon}D(B;s,e).
$$

即在保证接收安全的前提下，最小化最不利源位置和测向误差造成的后验定位直径。移动距离只作为次级比较指标。

## 5. 快速求解算法

连续优化采用“稀疏搜索 + 局部二次求解”。

### 5.1 稀疏候选生成

1. 计算 $\mathcal P_1^+$ 的最小包围圆圆心 $C$；
2. 以第一次示向方向为基准生成 12 个搜索方向；
3. 沿每个方向计算仍位于 $\mathcal U$ 内的最大步长；
4. 在最大步长的 $0.55$ 和 $0.90$ 处各取一点；
5. 连同圆心 $C$，形成约 25 个初始候选点。

这样避免在整个二维行动区域进行密集网格搜索。

### 5.2 粗评价

从源域边界点和内部积分点中抽取代表性源位置，并取

$$
e\in\{-\varepsilon,0,+\varepsilon\}.
$$

计算每个候选点的离散最坏直径，选择排名靠前的点作为局部搜索种子。

### 5.3 局部二次求解

当前实现选取前三个种子，在每个种子的八邻域搜索四轮，每轮将步长减半：

$$
h_{k+1}=\frac12h_k.
$$

只有仍满足安全约束的邻域点才会被评价。

### 5.4 最终复核

优良候选点使用更密的

$$
64\text{ 个源位置}\times 7\text{ 个误差节点}
$$

重新评价，最终输出前三个推荐点及其坐标、移动距离、安全余量、最坏后验直径和最坏状态见证。

## 6. 复杂度与下游复用

设候选点数为 $N_B$，源状态数为 $N_s$，误差节点数为 $N_e$，多边形顶点数为 $n$，主要复杂度约为

$$
O(N_BN_sN_en).
$$

由于各规模均受到控制，单场景可以在秒级完成。

Q3 每获得一次新观测后，可先用 Q1 更新源位置区域，再调用本模型选择下一测点。Q4 保留相同搜索框架，只需在源状态和反馈模型中加入干扰源朝向与盲区约束。

## 7. 结果解释边界

- 接收安全性是针对凸外包络的保守保证；
- 最坏定位直径是有限代表状态上的数值近似；
- 稀疏搜索和局部加密不声称得到连续空间严格全局最优；
- 完整曲线模型和高分辨率算法只作为离线复核工具。

中心测试场景约在 1.77 s 内完成，最坏后验直径由约 1500.23 m 降至约 111.90 m，说明轻量模型具有较好的定位压缩能力。
