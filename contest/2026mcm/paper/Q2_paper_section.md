# 四、问题二：第二检测点选择策略与候选区域优化

## 4.1 问题分析与决策准则

在问题二中，机器狗在第一检测点 $\mathbf{p}_1$ 测得某干扰源的初始示向度 $\hat\theta_1$。题设要求确定第二检测点 $\mathbf{p}_2$ 的选择策略与较优候选区域。第二检测点的选择需兼顾以下两点：

1. **信号接收保证**：干扰源覆盖半径 $R \in [1000, 1500]\text{ m}$ 未知。为获得不依赖未知实际覆盖半径的确定性接收保证，本文选取如下充分安全条件：第二检测点到该干扰源所有相容位置的距离均不得超过最小覆盖半径 $R_{\min} = 1000\text{ m}$。
2. **定位效果最优**：在先验分布未知的情况下，以两次交会后最坏情况下的定位多边形直径衡量定位效果，采用 Minimax 准则使该直径极小化。

选点以两次交会后的最坏后验定位效果为主目标，并将移动距离与安全余量作为次级优化准则。

---

## 4.2 初次观测后的目标可行域

设第一检测点坐标为 $\mathbf{p}_1 = (x_1, y_1)^\top$，实测示向度为 $\hat\theta_1$。初次观测后，干扰源真实位置 $\mathbf{s}$ 满足如下几何约束：

1. **测向角域**：$\mathbf{s} \in \mathcal{W}_1 = \mathcal{W}(\mathbf{p}_1, \hat\theta_1, \varepsilon)$，其中误差半宽 $\varepsilon = 1^\circ$；
2. **覆盖范围**：$\|\mathbf{s} - \mathbf{p}_1\|_2 \le R_{\max} = 1500\text{ m}$；
3. **目标区域**：$\|\mathbf{s}\|_2 \le R_{\text{target}} = 1800\text{ m}$；
4. **近场盲区**：$\|\mathbf{s} - \mathbf{p}_1\|_2 > r_{\text{near}} = 5\text{ m}$。

上述约束的交集即为初测后的目标可行域 $\mathcal{P}_1$。为便于计算，采用角域射线与圆边界切线构建包含 $\mathcal{P}_1$ 的凸多边形外包络 $\mathcal{P}_1^+$（满足 $\mathcal{P}_1 \subseteq \mathcal{P}_1^+$）。数值实现中，目标圆区域及最大接收圆均采用 48 边外切正多边形近似，以保证 $\mathcal{P}_1 \subseteq \mathcal{P}_1^+$ 的保守包含关系。在基准工况（$\mathbf{p}_1 = (0, 0)\text{ m}, \hat\theta_1 = 0^\circ$）下，$\mathcal{P}_1^+$ 为底边长 $52.36\text{ m}$、高 $1500\text{ m}$ 的等腰三角形。

---

## 4.3 保守安全区域构建

为保证在第二检测点 $\mathbf{p}_2$ 处必定接收到信号，定义信号接收安全余量：
$$
m(\mathbf{p}_2) = R_{\min} - \max_{\mathbf{s} \in \mathcal{P}_1^+} \|\mathbf{p}_2 - \mathbf{s}\|_2
$$
若考虑定位与控制误差，可设置缓冲距离 $b > 0$ 要求 $m(\mathbf{p}_2) \ge b$；本文基准模型取 $b = 0$。

由于欧氏距离关于点 $\mathbf{s}$ 具有凸性，点 $\mathbf{p}_2$ 到凸多边形 $\mathcal{P}_1^+$ 内任意点的最大距离必在其有限顶点集 $V(\mathcal{P}_1^+)$ 处取得：
$$
\max_{\mathbf{s} \in \mathcal{P}_1^+} \|\mathbf{p}_2 - \mathbf{s}\|_2 = \max_{\mathbf{v} \in V(\mathcal{P}_1^+)} \|\mathbf{p}_2 - \mathbf{v}\|_2
$$
据此，第二检测点的保守安全候选区域 $\mathcal{U}$ 为：
$$
\mathcal{U} = \left( \bigcap_{\mathbf{v} \in V(\mathcal{P}_1^+)} \left\{ \mathbf{p}_2 \in \mathbb{R}^2 : \|\mathbf{p}_2 - \mathbf{v}\|_2 \le R_{\min} \right\} \right) \setminus \{\mathbf{p}_1\}
$$
即 $\mathcal{U}$ 为以 $\mathcal{P}_1^+$ 各顶点为圆心、$R_{\min}$ 为半径的闭圆盘交集并排除第一检测点自身。排除 $\mathbf{p}_1$ 是由于题设同地点重复检测的环境误差保持不变，原地再次测量无法提供独立测向基线。只要 $\mathbf{p}_2 \in \mathcal{U}$，即可确保对可行域内的任意源位置均处于有效接收范围内。若 $\mathcal{U} = \varnothing$，表明在以 $R_{\min}$ 为充分安全约束下无可行测点，算法将给出无解标识并转入异常回退流程，避免盲目输出不可靠测点。

---

## 4.4 最坏后验直径选点优化模型与近优候选域

在安全区域 $\mathcal{U}$ 内，机器狗在 $\mathbf{p}_2$ 处观测可能出现两种情形：

1. 若第二检测点距干扰源真实位置不超过近场阈值（$\|\mathbf{p}_2 - \mathbf{s}\|_2 \le r_{\text{near}} = 5\text{ m}$），测向接口返回 `near`。机器狗随后可进入光学精确定位与清除流程，因此在第二测点选择目标中将该反馈视为终止分支，令其剩余位置不确定性为 $0$；
2. 若处于近场之外，测得示向度 $\hat\theta_2$。在测向误差 $e_2 \in [-\varepsilon, \varepsilon]$ 作用下，形成第二角域 $\mathcal{W}_2$，交会后的保守后验可行域为 $\mathcal{P}_2 = \mathcal{P}_1^+ \cap \mathcal{W}_2$。

据此，定义单次观测下的后验定位损失函数为：
$$
L(\mathbf{p}_2; \mathbf{s}, e) = \begin{cases}
0, & \|\mathbf{p}_2 - \mathbf{s}\|_2 \le r_{\text{near}} \\
\operatorname{diam}\left[ \mathcal{P}_1^+ \cap \mathcal{W}\left(\mathbf{p}_2, \operatorname{bearing}(\mathbf{p}_2, \mathbf{s}) + e, \varepsilon\right) \right], & \|\mathbf{p}_2 - \mathbf{s}\|_2 > r_{\text{near}}
\end{cases}
$$
在连续理论上，真实相容状态属于 $\mathcal{P}_1$。为保守覆盖源位置趋近 5 米盲区边界时的极限情形，连续评估将状态集合扩展至闭包 $\overline{\mathcal{P}}_1 = \operatorname{cl}(\mathcal{P}_1)$，数值采样点集相应满足 $S_N \subseteq \overline{\mathcal{P}}_1$。交会区域采用外包络 $\mathcal{P}_1^+$ 实施保守估计，定义保守最坏后验直径为：
$$
J^+(\mathbf{p}_2) = \sup_{\mathbf{s} \in \overline{\mathcal{P}}_1,\; |e| \le \varepsilon} L(\mathbf{p}_2; \mathbf{s}, e)
$$
式中明确区分了真实源状态空间 $\mathbf{s} \in \overline{\mathcal{P}}_1$ 与保守包络几何 $\mathcal{P}_1^+$。在数值解算时，采用有限采样集（$N$ 个代表源状态 $S_N \subset \overline{\mathcal{P}}_1$ 与 $M$ 个误差节点 $E_M$）计算离散评估指标：
$$
\widehat{J}_{N,M}(\mathbf{p}_2) = \max_{\mathbf{s}_i \in S_N,\; e_j \in E_M} L(\mathbf{p}_2; \mathbf{s}_i, e_j)
$$

经稀疏初搜与局部细化后，入围候选点进入高密度复核阶段（$N=64, M=7$）。设进入高密度复核的候选点集合为 $\mathcal{Q}_{\text{final}} \subset \mathcal{U}$，当前启发式搜索求得的最优评价值与数值推荐点定义为：
$$
\widetilde{J}_{64,7} = \min_{\mathbf{p} \in \mathcal{Q}_{\text{final}}} \widehat{J}_{64,7}(\mathbf{p}), \qquad \widetilde{\mathbf{p}}_2 \in \arg\min_{\mathbf{p} \in \mathcal{Q}_{\text{final}}} \widehat{J}_{64,7}(\mathbf{p})
$$
进入高密度复核阶段的候选点按 $(\widehat{J}_{64,7}(\mathbf{p}), \|\mathbf{p} - \mathbf{p}_1\|_2, -m(\mathbf{p}))$ 的字典序严格排序并优选。

为进一步量化给出“较优候选区域”，定义在空间采样网格 $\mathcal{G}_h$（网格步长 $h$）上相对容差 $\tau > 0$（如 $\tau = 1\%, 3\%, 5\%$）的近优候选区域：
$$
\widehat{\mathcal{C}}_{\tau, h} = \left\{ \mathbf{p} \in \mathcal{G}_h \cap \mathcal{U} : \widehat{J}_{N,M}(\mathbf{p}) \le (1+\tau)\widehat{J}_{N,M, h}^* \right\}
$$
式中 $\widehat{J}_{N,M, h}^* = \min_{\mathbf{p} \in \mathcal{G}_h \cap \mathcal{U}} \widehat{J}_{N,M}(\mathbf{p})$ 为全域网格扫描下的基准最优值。该区域给出了在定位性能损失受控前提下允许选点的几何范围。

---

## 4.5 快速求解算法

为兼顾全局探索与计算效率，采用“稀疏初搜—局部细化”两阶段搜索，并对入围候选点实施高密度复核：

1. **稀疏初搜**：以 $\mathcal{P}_1^+$ 的最小包围圆（半径记为 $r_{\mathrm{MEC}}$）圆心为基准向四周引出 12 个辐射方向，分别在安全射线最大长度的 $55\%$ 与 $90\%$ 处取样，连同圆心共生成至多 25 个初选候选点，并采用 24 个源点 $\times$ 3 个误差节点（$-\varepsilon, 0, \varepsilon$）进行粗评估；
2. **局部细化**：选取粗搜前 3 个种子点，以初始步长 $h_0 = \min\{200, \max[20, 0.45(R_{\min}-r_{\mathrm{MEC}})]\}\text{ m}$ 进行 4 轮八邻域步长折半搜索；
3. **高密度复核**：将粗搜前 6 点与局部细化点合并去重构建最终入围集 $\mathcal{Q}_{\text{final}}$，采用 64 个源点 $\times$ 7 个误差节点实施最终离散复核，输出字典序排序前三的优质候选点，并以首位点作为基准推荐点 $\widetilde{\mathbf{p}}_2$。

---

## 4.6 算例求解与多场景验证

记算法求得的最坏后验直径为 $\widetilde{D}_2 := \widetilde{J}_{64,7} = \widehat{J}_{64,7}(\widetilde{\mathbf{p}}_2)$，初始外包络几何直径为 $D_1^+ = \operatorname{diam}(\mathcal{P}_1^+)$，离散评估不确定性压缩比为 $\widetilde{\eta} = (D_1^+ - \widetilde{D}_2)/D_1^+$。对基准工况（$\mathbf{p}_1 = (0, 0)\text{ m}, \hat\theta_1 = 0^\circ$）进行求解，单次计算耗时约 1.2 秒，指标如表 4 所示。

### 表 4：基准工况下第二检测点优化解算指标

| 评估指标 | 符号 | 数值 | 量纲 | 说明 |
| :--- | :---: | :---: | :---: | :--- |
| 第一检测点位置 | $\mathbf{p}_1$ | $(0.00, 0.00)$ | $\text{m}$ | 初始观测位置 |
| 初始实测示向度 | $\hat\theta_1$ | $0^\circ$ | 度 | 沿 $+x$ 轴正方向 |
| 推荐第二检测点 | $\widetilde{\mathbf{p}}_2$ | $(839.64, 538.75)$ | $\text{m}$ | 算法启发式数值推荐点 |
| 机器狗移动距离 | $\|\widetilde{\mathbf{p}}_2 - \mathbf{p}_1\|_2$ | 997.62 | $\text{m}$ | 接近安全域边界，次级选点代价 |
| 保守安全余量 | $m(\widetilde{\mathbf{p}}_2)$ | 2.38 | $\text{m}$ | 满足非负条件，实际宜设缓冲 |
| 初测外包络跨度 | $D_1^+$ | 1500.23 | $\text{m}$ | 基准工况初始外包络直径 |
| 离散最坏后验直径 | $\widetilde{D}_2$ | **111.90** | $\text{m}$ | 64 源点 $\times$ 7 误差节点采样复核 |
| 离散评估压缩比 | $\widetilde{\eta}$ | **92.5%** | - | $(D_1^+ - \widetilde{D}_2)/D_1^+$ |
| 单例计算耗时 | $t_{\text{solve}}$ | $\sim 1.2$ | $\text{s}$ | 具备秒级快速选点能力 |

中心工况的连续模型关于第一示向线具有镜像对称性；有限代表状态求解得到两组近似镜像的高质量推荐点：
$$
\widetilde{\mathbf{p}}_2^{(1)} = (839.64, 538.75)\text{ m}, \quad \widetilde{\mathbf{p}}_2^{(2)} = (839.64, -538.75)\text{ m}
$$
两点移动距离均为 $997.62\text{ m}$，安全余量均为 $2.38\text{ m}$。受有限源点采样的离散误差影响，两点目标值存在约 $0.15\text{ m}$ 的微小数值差异（正侧 $111.90\text{ m}$，负侧 $112.04\text{ m}$）。当前算法不声称获得连续候选域上的严格全局最优解。在该测点下，离散最坏后验直径由初测的 $1500.23\text{ m}$ 降至 **111.90 m**，离散评估压缩比达 **92.5%**。

![第二检测点保守安全域与推荐测点示意图](mathmode/images/fig_q2_candidate_region.png)
*图 2：第二检测点保守安全域 $\mathcal{U}$ 与推荐测点几何分布图*

如图 2 所示，该示意图展示了保守安全域 $\mathcal{U}$ 的几何轮廓及优选测点位置。算法推荐测点位于第一示向线两侧，通过增大两次测向基线的方向差异，降低近共线交会导致的后验区域拉伸；其改善程度由后验直径数值直接评价，而不预设固定的最优交会角。

为初步检查快速模型对不同几何构型的适用性，进一步对圆心、偏置、切向及圆外等 6 大代表性工况开展实际求解，结果如表 5 所示。

### 表 5：快速模型在不同测试场景下的实际解算结果对比

| 测试场景 | 第一测点与示向 | 推荐测点 $\widetilde{\mathbf{p}}_2$ | 余量 | $D_1^+$ | $\widetilde{D}_2$ | 压缩比 | 耗时与场景说明 |
| :--- | :---: | :---: | :---: | :---: | :---: | :---: | :--- |
| 中心基准 | $(0, 0)\text{ m},\; 0^\circ$ | $(839.6, 538.8)\text{ m}$ | $2.38\text{ m}$ | $1500.2\text{ m}$ | $111.90\text{ m}$ | $92.5\%$ | $1.20\text{ s}$，对称交会 |
| 偏置场景 | $(300, 500)\text{ m},\; 125^\circ$ | $(-615.9, 898.5)\text{ m}$ | $1.16\text{ m}$ | $1493.9\text{ m}$ | $107.21\text{ m}$ | $92.8\%$ | $1.18\text{ s}$，内部非对称 |
| 边缘内指 | $(1600, 0)\text{ m},\; 180^\circ$ | $(760.4, -538.8)\text{ m}$ | $2.38\text{ m}$ | $1500.2\text{ m}$ | $111.90\text{ m}$ | $92.5\%$ | $1.22\text{ s}$，边界指心 |
| 边缘切向 | $(1600, 0)\text{ m},\; 90^\circ$ | $(1021.8, 605.0)\text{ m}$ | $163.20\text{ m}$ | $854.7\text{ m}$ | **41.41 m** | **95.2%** | $1.28\text{ s}$，目标圆截断 |
| 圆外内指 | $(2000, 0)\text{ m},\; 180^\circ$ | $(911.1, 454.0)\text{ m}$ | **0.27 m** | $1300.3\text{ m}$ | $85.87\text{ m}$ | $93.4\%$ | $1.30\text{ s}$，圆外测点余量极敏感 |
| 跨界回绕 | $(0, 0)\text{ m},\; 359.5^\circ$ | $(844.6, 531.3)\text{ m}$ | $2.20\text{ m}$ | $1500.5\text{ m}$ | $111.87\text{ m}$ | $92.5\%$ | $1.24\text{ s}$，角度平滑跨越 |

在基准测试中，六个代表性场景单次解算耗时为 $1.18\sim 1.30\text{ s}$，最坏后验直径较初始外包络显著压缩，表现出良好的鲁棒性与秒级实时决策能力。特别地，圆外场景的无缓冲推荐点安全余量仅为 $0.27\text{ m}$，说明以定位性能为主目标时最优点可能贴近安全边界，实际工程中应结合控制精度预留安全裕量。该快速选点策略兼顾了精度与效率，为问题三的多无人机协同搜索规划提供了可靠的决策基础。

---

## 4.7 近优域、运行稳定性与安全缓冲敏感性

### 4.7.1 近优候选区域的全域网格量化

为将“较优候选区域”落实为可执行范围，在基准工况下以 $h=25\text{ m}$ 对安全域实施全域网格扫描。扫描边界由各安全圆盘的公共轴对齐包围盒自动确定，共得到 702 个安全网格点；采用与稀疏初搜一致的 24 个代表源点和 3 个误差节点计算 $\widehat J_{24,3}$。网格最优值为 $111.893\text{ m}$，对应网格点 $(850,525)\text{ m}$。各近优域结果见表 6，其中面积按入选网格点数乘以 $h^2$ 估计，属于离散网格面积估计而非连续区域的精确解析面积。

### 表 6：基准工况近优候选域的 $25\text{ m}$ 网格统计

| 相对容差 $\tau$ | 直径阈值 | 网格点数 | 网格面积估计 | 8-连通分量 | 总包围盒 $(x,y)$ |
| :---: | :---: | :---: | :---: | :---: | :---: |
| $1\%$ | $113.012\text{ m}$ | 4 | $0.0025\text{ km}^2$ | 4 | $[800,850]\times[-600,600]\text{ m}$ |
| $3\%$ | $115.250\text{ m}$ | 14 | $0.00875\text{ km}^2$ | 2 | $[775,875]\times[-625,625]\text{ m}$ |
| $5\%$ | $117.488\text{ m}$ | 28 | $0.0175\text{ km}^2$ | 2 | $[750,900]\times[-625,625]\text{ m}$ |

$3\%$ 与 $5\%$ 近优域均形成关于初始示向线镜像对称的两个分离分量，定量支持“双侧近优选点带”的结论；$1\%$ 阈值下仅保留 4 个离散核心网格点，受网格分辨率影响表现为 4 个分量，不能据此断言其连续集合必然断裂。

![第二检测点近优候选区域全域网格扫描与等值线图](mathmode/images/fig_q2_candidate_contour.png)
*图 3：基准工况下近优候选区域的全域网格扫描与等值线分布*

### 4.7.2 六场景重复计时

为排除单次计时波动，对表 5 的六个场景分别独立执行 20 次完整解算。表 7 给出均值、标准差、中位数、P95 与最大值。各场景平均耗时为 $1.216\sim1.286\text{ s}$，P95 均不超过 $1.313\text{ s}$，120 次运行中的最大值为 $1.326\text{ s}$；各场景变异系数均低于 $2\%$。结果证实该算法具备高度稳定的秒级快速选点能力，可为后续动态在线决策提供可靠的原型支撑。

### 表 7：六场景各 20 次完整解算耗时统计（单位：s）

| 场景 | 均值 | 标准差 | 中位数 | P95 | 最大值 |
| :--- | ---: | ---: | ---: | ---: | ---: |
| 中心基准 | 1.224 | 0.019 | 1.223 | 1.253 | 1.270 |
| 偏置场景 | 1.216 | 0.012 | 1.214 | 1.232 | 1.250 |
| 边缘内指 | 1.238 | 0.016 | 1.234 | 1.273 | 1.275 |
| 边缘切向 | 1.286 | 0.017 | 1.284 | 1.313 | 1.326 |
| 圆外内指 | 1.264 | 0.009 | 1.264 | 1.282 | 1.286 |
| 跨界回绕 | 1.268 | 0.010 | 1.265 | 1.286 | 1.292 |

### 4.7.3 正安全缓冲的性能代价与可行边界

令安全要求由 $m(\mathbf p_2)\ge0$ 收紧为 $m(\mathbf p_2)\ge b$。实现中仅将选点安全半径改为 $R_{\min}-b$，初次观测状态域仍按题设物理参数 $R_{\min}=1000\text{ m}$ 构建，从而避免把控制缓冲误当作源状态参数。中心工况结果见表 8。

### 表 8：中心工况的安全缓冲敏感性

| $b$ | 推荐测点 $(x,y)$ | 实际安全余量 $m$ | $\widetilde D_2$ | 相对 $b=0$ 增幅 |
| :---: | :---: | :---: | :---: | :---: |
| $0\text{ m}$ | $(839.6,538.8)\text{ m}$ | $2.38\text{ m}$ | $111.90\text{ m}$ | $0$ |
| $5\text{ m}$ | $(837.9,532.6)\text{ m}$ | $7.20\text{ m}$ | $112.85\text{ m}$ | $0.85\%$ |
| $10\text{ m}$ | $(836.1,526.4)\text{ m}$ | $12.03\text{ m}$ | $113.47\text{ m}$ | $1.40\%$ |
| $20\text{ m}$ | $(832.5,513.8)\text{ m}$ | $21.71\text{ m}$ | $115.37\text{ m}$ | $3.11\%$ |

可见，中心工况预留 $10\text{ m}$ 缓冲时，最坏后验直径增加 $1.57\text{ m}$，代价为 $1.40\%$。进一步地，若 $r_{\mathrm{MEC}}$ 为 $\mathcal P_1^+$ 的最小外接圆半径，则排除同点约束前的安全圆盘交非空的充要条件为
$$
b\le b_{\max}=R_{\min}-r_{\mathrm{MEC}}.
$$
圆外内指场景中 $r_{\mathrm{MEC}}=650.268\text{ m}$，故 $b_{\max}=349.732\text{ m}$。数值扫描在 $b=349\text{ m}$ 时仍返回可行点，而 $b=350\text{ m}$ 时返回 `NO_CONSERVATIVELY_SAFE_CANDIDATE`，与该几何阈值一致。需要强调，表 5 中 $0.27\text{ m}$ 是在仅要求非负余量时，算法以最坏后验直径极小化为主目标导致优选点贴近安全边界，并非整个安全域能够承受的最大缓冲。随着 $b$ 逐步增大逼近 $b_{\max}$，候选区域向最小外接圆圆心收缩，后验直径随之单调增大，客观揭示了物理安全性与定位几何分辨力之间的权衡关系。本问建立的第二检测点快速优化选点模型与安全域量化分析，为问题三中机器狗自动搜索定位与在线机动决策提供了坚实可靠的算法与理论支撑。
