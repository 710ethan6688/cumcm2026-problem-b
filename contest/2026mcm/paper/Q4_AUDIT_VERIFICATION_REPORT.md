# Q4 论文审查修改落实与终审决议请求简报（致 Codex）

> **【Token 限额硬性约束与阅读指引】**
> 1. **严禁读取 PDF 或大型图像**：所有修改已直接更新至 Markdown，请直接阅读同目录下的全新 [`Q4_paper_section.md`](Q4_paper_section.md)！
> 2. **严格控制 Token 消耗**：请务必保留充足 Token 配额，直奔主题，无需客套寒暄。
> 3. **核心任务**：复核上轮审查报告中全部 17 项修改的闭环落实情况，给出最终是否达到冻结标准的终审决议。

---

## 一、 上轮 17 项审查意见系统性落实清单

针对你在 [`Q4_CODEX_AUDIT_REPORT.md`](Q4_CODEX_AUDIT_REPORT.md) 中指出的 17 项问题，我们已在正文（`Q4_paper_section.md` 及编译源码 `main.tex`）中逐项彻底完成闭环修正：

### 1. 四类无信号原因两两互斥化（第 6.2.2 节）
引入清除指示变量 $h_c^k \in \{0, 1\}$，定义活动指示变量 $a_c^k = e_c (1 - h_c^k)$，将无信号观测严格分解为四类两两互斥的物理事件之并：
$$\{y_c^k(\mathbf{q}) = \texttt{no\_signal}\} = E_0 \cup E_1 \cup E_2 \cup E_3, \quad E_i \cap E_j = \emptyset \; (\forall i \neq j)$$
- $E_0 = \{e_c = 0\}$（未布设目标）；
- $E_1 = \{e_c = 1, h_c^k = 1\}$（已清除目标）；
- $E_2 = \{a_c^k = 1, \|\mathbf{q} - \mathbf{x}_c\| > R_c\}$（活动目标超距）；
- $E_3 = \{a_c^k = 1, \|\mathbf{q} - \mathbf{x}_c\| \le R_c, \tau_c = \mathrm{D}, \mathbf{u}_c^\top (\mathbf{q} - \mathbf{x}_c) < 0\}$（活动目标在视距内但处于盲侧）。
观测变量统一时间索引为 $y_c^k(\mathbf{q})$；明确声明 $e_c = 0$ 时后序空间分量无物理定义。

### 2. 动作时间口径与赛题严格统一（第 6.2.3 节）
彻底纠正简报笔误，正文与赛题规程统一为：
- 移动耗时 $L/v$（$v = 5\text{ m/s}$）；天线调谐切频 $1\text{ s}$；停靠测向 $5\text{ s}$；
- 激光清除成功耗时 $5\text{ s}$（$3\text{ s}$ 光学精确定位 $+ 2\text{ s}$ 激光发射）；激光清除失败耗时 $3\text{ s}$（未探测到目标）。客观运行时间受赛题规程约束：$T_{\mathrm{wall}} \le 20\text{ min}$。

### 3. 命题 5 必要性证明符号方向修正（第 6.3.1 节）
- 修正取反错误：严格分离超平面给出非零向量 $\mathbf{v}$ 使 $\forall \mathbf{q} \in C = \operatorname{conv}(\mathcal{S}_r(\mathbf{x}))$ 恒有 $\mathbf{v}^\top (\mathbf{q} - \mathbf{x}) < 0$；选取目标辐射朝向 $\mathbf{u} = \mathbf{v}/\|\mathbf{v}\|$，则所有局部测站恒有 $\mathbf{u}^\top (\mathbf{q}_i - \mathbf{x}) < 0$，反例成立，所有测站均落入开盲侧；
- 明确指出 $\mathcal{S}_r(\mathbf{x})$ 有限，故 $C$ 为紧闭凸集，严格分离定理适用；命题结论修改为“属于闭凸包”。

### 4. 25 点拓扑四类边长与全域几何包络证明补齐（第 6.3.1 节）
正文显式列出 36 个三角形内部全部四类连接边长：
1. 中心至内环（12 条）：$d_1 = R_1 = 950\text{ m}$；
2. 内环相邻边（12 条）：$d_2 = 2 R_1 \sin(15^\circ) \approx 491.76\text{ m}$；
3. 外环相邻边（12 条）：$d_3 = 2 R_2 \sin(15^\circ) \approx 968.02\text{ m}$；
4. 内外环相邻跨接边（24 条）：$d_4 = \sqrt{R_1^2 + R_2^2 - 2 R_1 R_2 \cos(15^\circ)} \approx 983.60\text{ m}$。
由此证明 $\max = 983.60\text{ m} < 1000\text{ m}$；对区域内任意点，其到所在三角形 3 顶点的距离均不超过该单元最长边，故 3 顶点均属于 $\mathcal{S}_{1000}(\mathbf{x})$ 且将其包含在凸包内；外环正十二边形内切圆 $R_2 \cos(15^\circ) \approx 1806.28\text{ m} > 1800\text{ m}$，实现对搜索圆域的完全包络。

### 5. 表 12 损坏数据与定界符全部恢复（第 6.3.1 节）
所有数据完整恢复：外环内切圆半径 $1806.28\text{ m}$、剖分三角形最大边长 $983.60\text{ m}$、探测裕度 $16.40\text{ m}$、数学定界符成对闭合。

### 6. 距离安全准则提升为凸多边形顶点极值（第 6.3.2 节）
删去仅依赖离散样本的表述，利用欧氏距离函数 $\|\mathbf{q} - \mathbf{x}\|$ 的凸性，其在紧致凸多边形 $P_c^k$ 上的最大值必在顶点集取得：
$$d_{\max}(\mathbf{q}, P_c^k) = \max_{\mathbf{x} \in P_c^k} \|\mathbf{q} - \mathbf{x}\| = \max_{\mathbf{v} \in \operatorname{Vert}(P_c^k)} \|\mathbf{q} - \mathbf{v}\|$$
对顶点集校验满足 $\le 995\text{ m}$，即可在数学上确证对多边形内部任意真实目标位置均满足 $\|\mathbf{q} - \mathbf{x}_c\| \le 995\text{ m} < 1000\text{ m} \le R_c$，严格排除超距可能。

### 7. 引理 1 显式列明真值包含前提（第 6.3.2 节）
显式列明：初始多边形覆盖目标区域与最大覆盖圆、测向角楔严格覆盖真实方位角（$[-1^\circ, 1^\circ]$ 且预留 $1.005^\circ$ 容差）、仅使用有效正示向通过半平面求交更新，以此确立包含性递推成立。

### 8. 双阈值物理与工程内涵分层澄清（第 6.3.3 节）
明确区分：$\rho \le 20.0\text{ m}$ 为理论充分清除半径；常规阶段采用 $\rho \le 19.5\text{ m}$ 作为提前触发工程阈值，预留 $0.5\text{ m}$ 的数值计算与机动执行工程裕量；尾部追加探测后若后验半径收敛至理论边界 $\rho \le 20.0\text{ m}$，则直接机动至圆心清除。

### 9. 26 米正方形网格实现条件补齐（第 6.3.3 节）
明确网格剖分保留所有与 $P_c^k$ 存在几何相交的完整网格单元（而非仅中心在内的单元），单元半对角线 $18.385\text{ m} < 20.0\text{ m}$，有限步遍历相交单元中心保证必中目标。

### 10. 准则 3 修正为充分退出证书（第 6.3.4 节）
删去“当且仅当”，修正为“在且仅在以下两项充分证书之一满足时”签发证书退出；第二分支写明逐频道确证要求：所有捕获目标已清除，且其余频道在 25 点基准测站中均遍历检测且全部明确返回 $\texttt{no\_signal}$。

### 11. 镜像公式定位为工程恢复候选（第 6.4.1 节）
将镜像重捕获准确定位为在探测遇到盲侧时的轻量几何恢复候选（提高落入对侧有效发射半平面的概率），并重新校验顶点距离安全，不作绝对物理对称或必然成功等夸大声明。

### 12. 判据 3 定义单步前瞻成本权衡（第 6.4.1 节）
定义相容状态集合 $\mathcal{H}_k$ 与候选测站的几何相容状态代理量 $p_v(\mathbf{q}) = \#\{(\mathbf{x}_i, \mathbf{u}_j) \in \mathcal{H}_k \mid \mathbf{q} \text{ 可接收}\} / \#\mathcal{H}_k$；将其准确定位为基于几何代理与单步前瞻成本权衡的启发式触发判据，删去严格期望时间推导声明。

### 13. 算法 2 与命题 6 规范化表述（第 6.4.2 & 6.4.3 节）
- 算法 2 改为规范的中文步骤叙述，彻底消除代码式保留字；
- 命题 6 补全理想同步执行前提（目标静止、单源单频、无外部通信中断），结论限定为在有限离散动作步数内完成全部实际存在目标的清除并安全退出，不推导真实时钟绝对界。

### 14. 表 14 与表 15 缺失数据与转义彻底修复（第 6.5 节）
修复了此前 Python 脚本在 Markdown 转换中将 `\1806`、`\7323` 等数字误判为正则反向引用组的缺陷，所有实验数据完整还原：
- 表 14：V2 均值 $7323.1\text{ s}$ vs V3 均值 $7213.3\text{ s}$，配对差 $-109.8\text{ s}$，95% 置信区间 $[-158.7, -60.9]\text{ s}$，最劣场景 $10094.6\text{ s}$ vs $9448.1\text{ s}$（降低 $646.5\text{ s}$），无效尝试 $46.78$ vs $27.99$ 次（减少 40.2%），胜平负统计低 35 场、持平 59 场、高 6 场；
- 表 15：17 轮演练全场清除率 100%（221 个源全部清除），加权平均单源耗时 $530.41\text{ s}$，中位数 $502.43\text{ s}$。

### 15. 实验归因客观化（第 6.5.1 节）
删去“尾部机制在 35 场复杂难例中成功触发”的未证归因，客观表述为“在 35 场触发尾部机制的样本中……样本集中最长耗时减少 $646.5\text{ s}$”。

### 16. 表 16 预留结构澄清（第 6.5.2 节）
明确表 16 为赛题规范中三次正式测试的标准输出结构预留，演练与正式测试概念严格解耦。

### 17. 语言规范与学术军规全面达标（全篇）
- **零自夸**：彻底删去“大幅压缩”、“极端难例大幅降低”、“有力验证”、“全面检验”、“坚决摒弃黑盒假设”、“数学严密”、“极佳的实时计算效率”、“理论上杜绝目标遗漏”等修饰，全部替换为客观学术表达；
- **零无关术语**：删除第 7 节中与正文缺乏映射的“荣格定理”、“旋转卡壳法”；
- **模型推广适度化**：将推广语境收敛至“基于有界视角与半平面辐射的固定多源在线搜索任务”；
- **页面严格 30 页**：经 XeLaTeX 重新编译，`main.pdf` 最终页数**严格控制在 30 页**（第 30 页末尾收录全部 10 篇参考文献及 `\CumcmAIUsed` 声明，0 报错，无溢出）。

---

## 二、 终审决议请求

请根据最新版 [`Q4_paper_section.md`](Q4_paper_section.md)，复核上述 17 项修改是否已全部合格闭环，并给出明确终审决议（如“正式确认：Q4正文达到冻结标准”）。
